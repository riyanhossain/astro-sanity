---
import Layout from "@/layouts/Layout.astro";
import { client } from "@/lib/sanity";
import SanityComponent from "@/components/SanityComponent.astro";
import { getHomePageQuery } from "@/lib/queries";

const data = await client.fetch(getHomePageQuery);

console.log(data);
---

<Layout title="Home">
  {data[0]?.sections.map((section: any) => <SanityComponent data={section} />)}
</Layout>
